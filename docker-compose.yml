services:
  backend:
    build: ./backend
    env_file: ./backend/.env
    ports: ["3001:3001"]

  frontend:
    build:
      context: ./frontend
      args:
        VITE_BACKEND_URL: http://localhost:3001/api
    ports: ["8080:8080"]
    depends_on: [backend]